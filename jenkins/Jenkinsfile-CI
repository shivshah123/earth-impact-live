pipeline {
    agent any

    environment {
        ARTIFACT_NAME = "earth-impact-live.zip"
        SONAR_PROJECT_KEY = "earth-impact-live"
        SONAR_PROJECT_NAME = "Earth Impact Live"
    }

    stages {
        stage('Clone Source Code') {
            steps {
                git branch: 'main', url: 'https://github.com/shivshah123/earth-impact-live.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                bat 'npm install'
            }
        }

        stage('SonarQube Code Quality Scan') {
            steps {
                withSonarQubeEnv('SonarQube-Server') {
                    bat '''
                    sonar-scanner ^
                      -Dsonar.projectKey=%SONAR_PROJECT_KEY% ^
                      -Dsonar.projectName="%SONAR_PROJECT_NAME%" ^
                      -Dsonar.sources=. ^
                      -Dsonar.exclusions=node_modules/**,terraform/**,coverage/**,*.zip
                    '''
                }
            }
        }

        stage('Trivy Security Scan') {
            steps {
                bat '''
                trivy fs . --severity HIGH,CRITICAL --ignore-unfixed --exit-code 0
                '''
            }
        }

        stage('Build Application') {
            steps {
                bat 'npm run build'
            }
        }

        stage('Run Tests') {
            steps {
                bat 'npm test'
            }
        }

        stage('Package Application') {
            steps {
                bat '''
                if exist %ARTIFACT_NAME% del %ARTIFACT_NAME%

                powershell -Command "Compress-Archive -Path server.js,package.json,package-lock.json,public,routes,controllers,services,models,config -DestinationPath %ARTIFACT_NAME% -Force"
                '''
            }
        }

        stage('Publish Artifact') {
            steps {
                archiveArtifacts artifacts: "${ARTIFACT_NAME}", fingerprint: true
            }
        }
    }
}